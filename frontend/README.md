# Frontend

Next.js app for the AI Technical Interviewer. See the [root README](../README.md) for setup.

```bash
npm install
npm run dev   # expects the backend on http://127.0.0.1:8000 (override with BACKEND_URL)
```
